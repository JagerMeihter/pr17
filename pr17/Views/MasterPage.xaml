<Page x:Class="pr17.MasterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Панель мастера"
      Background="#1E1E2F">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,25">
            <TextBlock Text="Панель мастера" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="#3498DB"/>

            <TextBlock x:Name="txtMasterName" 
                       FontSize="18" 
                       Foreground="#AAAAAA"
                       Margin="25,8,0,0"/>
        </StackPanel>

        <!-- Список записей мастера -->
        <ListView x:Name="lvMyAppointments" 
                  Grid.Row="1"
                  Background="Transparent" 
                  BorderBrush="#34495E"
                  Foreground="White"
                  Margin="5">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="#2C3E50" 
                            CornerRadius="10" 
                            Padding="16" 
                            Margin="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="{Binding Client.FullName}" 
                                           FontSize="17" 
                                           FontWeight="Bold"
                                           Foreground="White"/>
                                <TextBlock Text="{Binding ServiceType.Name}" 
                                           FontSize="16"
                                           Foreground="#3498DB"
                                           Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding DateTime, StringFormat='dd.MM.yyyy в HH:mm'}" 
                                           Foreground="#AAAAAA"/>
                                <TextBlock Text="{Binding Comment}" 
                                           Foreground="#777777"
                                           TextWrapping="Wrap"
                                           Margin="0,8,0,0"/>
                            </StackPanel>

                            <Button Content="Завершить услугу" 
                                    Grid.Column="1"
                                    Width="170"
                                    Height="45"
                                    VerticalAlignment="Center"
                                    Background="#27AE60"
                                    Foreground="White"
                                    FontSize="14"
                                    Click="BtnComplete_Click"
                                    Tag="{Binding}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Если записей нет -->
        <TextBlock x:Name="txtNoAppointments" 
                   Text="У вас пока нет записей" 
                   FontSize="18" 
                   Foreground="#777777" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>
    </Grid>
</Page>